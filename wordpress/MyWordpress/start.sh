#!/bin/bash

# Resumes containers previously stopped by docker-compose stop.

# Resumes services that have been previously stopped, to allow for smooth continuation of the operation at hand.

# Example:
docker-compose start
