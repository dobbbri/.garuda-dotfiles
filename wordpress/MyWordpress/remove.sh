#!/bin/bash

# Stops and removes the containers, networks, volumes, and images that were created by docker-compose-up.

# Shut down the whole stack of Docker applications gracefully and clean up the resources back to the initial state.

# Example:
sudo docker-compose down --volumes
